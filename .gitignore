node_modules/
dist/
apps/desktop/dist/
apps/desktop/release/
apps/desktop/build/update-gh-token
data/
.env
*.log
.DS_Store

# Private planning notes — keep local, never push
docs/saas-plan.md

# Per-tenant provisioning output — contains LiteLLM virtual keys
deploy/fly/tenants/

# Local Fly deploy secrets (Stripe, OpenRouter, LiteLLM master key)
deploy/fly/.deploy-secrets.local

# Desktop pack staging tree (generated copy of repo for Electron builds)
apps/desktop/pack-staging/

# Tauri (model-manager)
model-manager/src-tauri/target/
model-manager/src-tauri/gen/

# Voice server (Python)
voice-server/.venv/
__pycache__/

# Menubar tasks (Swift) build artifacts
apps/menubar-tasks/.build/
apps/menubar-tasks/ArcoMenubarTasks.app/
