{
  "id": "core.drive",
  "name": "Drive",
  "version": "0.1.0",
  "description": "Your documents — a view over the OS file store (os.files@1). Files created by the agent or any app appear here; swap Drive for any app speaking the same contract and nothing is lost.",
  "icon": "hard-drive",
  "tier": "code",
  "entry": { "kind": "bundle", "path": "drive" },
  "permissions": [
    { "kind": "contract", "id": "os.files@1", "access": "read" },
    { "kind": "contract", "id": "os.files@1", "access": "write" },
    { "kind": "shell", "features": ["notify"] }
  ],
  "events": { "subscribes": ["files.changed"] }
}
