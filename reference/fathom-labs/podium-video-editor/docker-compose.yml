version: '3.8'
services:
  web:
    build:
      context: ./
    volumes:
      - .:/app
      - ./app/node_modules
    command: npm run dev
    ports:
      - "3001:3001"
    privileged: true
    environment:
      NODE_ENV: development