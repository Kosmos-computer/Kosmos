/**
 * STUB: replace with useMusicStore selectors when a streaming provider API exists.
 * Re-exported for MusicParts compatibility.
 */
export { formatMusicTime, useMusicViewModel as useMusicStub, type MusicViewModel } from "./musicStore";
