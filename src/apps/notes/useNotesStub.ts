/**
 * @deprecated Use useNotesStore — Drive-backed Local / Kosmos Cloud vaults.
 * Kept as a thin re-export so older imports keep working during the migration.
 */
export { useNotesStore as useNotesStub, type NotesViewModel } from "./useNotesStore";
